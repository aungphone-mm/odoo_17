<?xml version="1.0" encoding="utf-8"?>
<!-- views/account_payment_register_views.xml -->
<odoo>
    <record id="view_account_payment_register_form_inherit" model="ir.ui.view">
    <field name="name">account.payment.register.form.inherit</field>
    <field name="model">account.payment.register</field>
    <field name="inherit_id" ref="account.view_account_payment_register_form"/>
    <field name="arch" type="xml">
        <!-- Add custom exchange rate fields with proper layout and visibility -->
        <xpath expr="//field[@name='currency_id']" position="after">
            <field name="show_custom_rate" invisible="1"/>
            <field name="source_amount" invisible="1"/>

            <!-- USD to MMK Exchange Rate panel -->
            <group string="Exchange Rate (USD to MMK)" invisible="not show_custom_rate">
                <field name="use_custom_exchange_rate"/>
                <label for="custom_exchange_rate" string="Exchange Rate (1 USD =)"
                       invisible="not use_custom_exchange_rate"/>
                <div class="d-flex" invisible="not use_custom_exchange_rate">
                    <field name="custom_exchange_rate"
                           class="oe_inline"
                           required="use_custom_exchange_rate"
                           placeholder="Enter exchange rate"/>
                    <span class="mx-2">MMK</span>

                    <!-- Simple Apply Rate button (no dropdown) -->
<!--                    <button name="apply_custom_rate_button"-->
<!--                            string="Apply Rate"-->
<!--                            type="object"-->
<!--                            class="btn-secondary"-->
<!--                            invisible="custom_exchange_rate == 0"/>-->

                </div>
                <field name="exchange_amount_preview"
                       widget="monetary"
                       options="{'currency_field': 'currency_id'}"
                       invisible="not use_custom_exchange_rate or custom_exchange_rate == 0"/>
            </group>
        </xpath>
    </field>
</record>

    <!-- Also show the stored rate in the payment form -->
    <record id="view_account_payment_form_inherit" model="ir.ui.view">
        <field name="name">account.payment.form.inherit</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='currency_id']" position="after">
                <field name="custom_exchange_rate"
                       invisible="custom_exchange_rate == 0"
                       groups="base.group_no_one"/>
            </xpath>
        </field>
    </record>
</odoo>